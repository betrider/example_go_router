name: go_router_builder
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  build: ^2.4.1
  source_gen: ^1.4.0
  analyzer: ^6.2.0
  collection: ^1.17.2

dev_dependencies:
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
